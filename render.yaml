services:
  - type: web
    name: revenex
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m revenex.api.http_server
    healthCheckPath: /health
    envVars:
      - key: HOST
        value: 0.0.0.0
